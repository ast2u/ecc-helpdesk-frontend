const baseApiUrl = 'http://localhost:8080';

export const environment = {
  baseApiUrl,
  apiEmployeeUrl: `${baseApiUrl}/api/employees`,
  apiTicketsUrl: `${baseApiUrl}/api/tickets`,
  apiRolesUrl: `${baseApiUrl}/api/employee_roles`,
};
